[project]
name = "mads-transformer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mlflow>=3.13.0",
    "ray[tune]>=2.55.1",
    "torch>=2.12.0",
    "torchvision>=0.27.0",
]
